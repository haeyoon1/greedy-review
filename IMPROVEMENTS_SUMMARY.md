# 🎨 Greedy Review UI/UX 개선 완료 보고서

## 📋 개선 요청사항

1. **Detail 페이지**
   - ✅ 코드 스니펫을 먼저, 코멘트를 나중에 표시
   - ✅ Markdown 문법 지원 (bold, 백틱, 리스트 등)
   - ✅ 키워드 하이라이트 유지
   - ✅ 페이지네이션 추가 (무한스크롤 대신)

2. **Home 페이지**
   - ✅ 3개의 레포지토리 탭 구성
   - ✅ 각 미션별 워드클라우드 분리
   - ✅ GitHub 링크 추가

---

## ✨ 주요 변경사항

### 1️⃣ Detail 페이지 개선

#### 📐 레이아웃 순서 변경
**이전:** 코멘트 → 파일 경로 → 코드 스니펫
**현재:** 파일 경로 → **코드 스니펫** → **코멘트**

```tsx
// Detail.tsx (리뷰 카드 구조)
<div className="review-content">
  {/* 1️⃣ 파일 경로 */}
  {review.file_path && <div className="file-path">...</div>}

  {/* 2️⃣ 코드 스니펫 (먼저!) */}
  {review.code_snippet && <DiffCodeBlock code={review.code_snippet} />}

  {/* 3️⃣ 코멘트 (나중에!) */}
  <MarkdownComment text={review.comment} keyword={keyword} />
</div>
```

#### 📝 Markdown 렌더링 개선
- **라이브러리:** `react-markdown` + `remark-gfm` + `rehype-raw`
- **지원 기능:**
  - ✅ Bold (`**굵은 글씨**`)
  - ✅ Inline code (`` `코드` ``)
  - ✅ 리스트 (ul/ol)
  - ✅ 링크, 이미지
  - ✅ 키워드 하이라이트 (`<mark>`)
  - ✅ 줄바꿈 (`\n` → 실제 줄바꿈)

```tsx
// MarkdownComment 컴포넌트
<ReactMarkdown remarkPlugins={[remarkGfm]} rehypePlugins={[rehypeRaw]}>
  {content.replace(/\\r\\n/g, "\n").replace(/\\n/g, "\n")}
</ReactMarkdown>
```

#### 📄 페이지네이션 추가
- **한 페이지당:** 5개 리뷰
- **기능:**
  - 페이지 번호 버튼
  - 이전/다음 화살표
  - `...` 생략 표시 (7페이지 초과시)
  - 현재 페이지 하이라이트 (초록색)

**스타일:**
- 초록색 활성 상태
- 호버 효과 (연한 초록 배경)
- 비활성 화살표 (opacity 30%)

---

### 2️⃣ Home 페이지 재구성

#### 🎯 3개의 레포지토리 탭

| 미션 | 이모지 | 설명 | GitHub |
|------|--------|------|--------|
| **자동차 경주** | 🏎️ | 원시값과 일급 컬렉션을 활용한 자동차 경주 게임 | [next-step/java-racingcar](https://github.com/next-step/java-racingcar) |
| **로또** | 🎰 | TDD와 OOP를 적용한 로또 번호 생성기 | [next-step/java-lotto](https://github.com/next-step/java-lotto) |
| **사다리 타기** | 🪜 | 함수형 프로그래밍을 활용한 사다리 게임 | [next-step/java-ladder-func-playground](https://github.com/next-step/java-ladder-func-playground) |

#### 🎨 UI 구조

```
┌─────────────────────────────────────────────┐
│  📊 코드 리뷰 키워드 분석                     │
│  넥스트 스텝 Java 미션 코드 리뷰...           │
└─────────────────────────────────────────────┘

┌──────────┬──────────┬──────────┐
│ 🏎️       │ 🎰       │ 🪜       │  ← 탭 버튼
│ 자동차   │ 로또     │ 사다리   │
│ 경주     │          │ 타기     │
└──────────┴──────────┴──────────┘

┌─────────────────────────────────────────────┐
│ 🎰 로또 미션                                 │  ← 현재 선택
│ TDD와 OOP를 적용한 로또 번호 생성기          │
│ [GitHub에서 보기]                            │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│                                              │
│        워드 클라우드 (초록색 테마)            │  ← 선택된 레포만
│                                              │
└─────────────────────────────────────────────┘

┌──────┬──────┬──────┐
│ 📊   │ 🔥   │ 💬   │  ← 통계 카드
│ 15   │ 42   │ 180  │
└──────┴──────┴──────┘
```

#### 🔄 동적 데이터 로딩

```tsx
const [selectedRepo, setSelectedRepo] = useState<Repository>("java-lotto");

useEffect(() => {
  setLoading(true);
  fetchKeywordStats(selectedRepo)  // 레포별 필터링!
    .then((stats) => { /* ... */ })
    .finally(() => setLoading(false));
}, [selectedRepo]);
```

---

### 3️⃣ API 개선

#### 레포지토리 필터링 추가

**[reviews.ts](frontend/src/api/reviews.ts:30-80)**

```typescript
// 이전
export async function fetchKeywordStats() {
  const reviews = await fetchReviews();
  // 전체 리뷰 분석
}

// 현재
export async function fetchKeywordStats(repo?: string) {
  let reviews = await fetchReviews();

  // 레포지토리 필터링
  if (repo) {
    reviews = reviews.filter((r) => r.repo === repo);
  }

  // 키워드 분석...
}
```

**사용 예시:**
```typescript
fetchKeywordStats("java-lotto")       // 로또만
fetchKeywordStats("java-racingcar")   // 자동차 경주만
fetchKeywordStats()                   // 전체
```

---

## 🎨 UI 스타일링

### Detail 페이지

#### Pagination CSS
```css
.pagination {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--spacing-sm);
  margin-top: var(--spacing-xl);
}

.pagination-number.active {
  background-color: var(--color-primary);  /* 초록 */
  color: var(--color-white);
  font-weight: 700;
}

.pagination-arrow:disabled {
  opacity: 0.3;
  cursor: not-allowed;
}
```

### Home 페이지

#### Repository Tabs
```css
.repo-tab {
  border: 2px solid var(--color-border);
  transition: all var(--transition-base);
}

.repo-tab:hover {
  border-color: var(--color-primary-light);
  transform: translateY(-2px);
}

.repo-tab.active {
  border-color: var(--color-primary);
  background: linear-gradient(
    135deg,
    var(--color-primary-pale) 0%,
    var(--color-white) 100%
  );
}
```

#### Current Repository Card
```css
.current-repo-card {
  background: linear-gradient(
    135deg,
    var(--color-bg) 0%,
    var(--color-primary-pale) 100%
  );
  border: 2px solid var(--color-primary);
}

.github-link {
  background-color: var(--color-black);
  color: var(--color-white);
}
```

---

## 📱 반응형 디자인

### 모바일 (< 768px)
- ✅ 레포지토리 탭: 3열 → **1열**
- ✅ 이모지 크기 축소: 2.5rem → **2rem**
- ✅ 워드클라우드 높이 축소: 400px → **300px**
- ✅ 페이지네이션: 자동 줄바꿈

---

## 📊 파일 변경 내역

### 신규 파일
없음 (기존 파일만 수정)

### 수정된 파일

| 파일 | 변경 내용 |
|------|----------|
| **[Detail.tsx](frontend/src/pages/Detail.tsx)** | ✅ 레이아웃 순서 변경<br>✅ 페이지네이션 추가<br>✅ Pagination 컴포넌트 구현 |
| **[Detail.css](frontend/src/pages/Detail.css)** | ✅ Pagination 스타일 추가 (80줄) |
| **[Home.tsx](frontend/src/pages/Home.tsx)** | ✅ 3개 레포지토리 탭 추가<br>✅ 현재 레포 카드 추가<br>✅ GitHub 링크 추가 |
| **[Home.css](frontend/src/pages/Home.css)** | ✅ 레포 탭 스타일<br>✅ 현재 레포 카드 스타일<br>✅ 반응형 개선 |
| **[reviews.ts](frontend/src/api/reviews.ts)** | ✅ `fetchKeywordStats(repo?)` 파라미터 추가<br>✅ 레포별 필터링 로직 |

---

## 🚀 사용 방법

### 1. Home 페이지
1. **미션 선택:** 3개의 탭 중 하나 클릭 (기본: 로또)
2. **워드클라우드 확인:** 선택한 미션의 키워드만 표시
3. **키워드 클릭:** Detail 페이지로 이동

### 2. Detail 페이지
1. **코드 먼저 확인:** 파일 경로 → 코드 스니펫 순서
2. **코멘트 읽기:** Markdown 렌더링 + 키워드 하이라이트
3. **페이지 이동:** 하단 페이지네이션 사용 (5개씩)
4. **PR 링크:** 각 리뷰마다 GitHub PR 링크

---

## 🎯 개선 효과

### Before
- ❌ 코멘트가 먼저 나와서 코드 맥락 파악 어려움
- ❌ 무한스크롤 → 특정 리뷰 찾기 어려움
- ❌ 단일 워드클라우드 → 미션별 분석 불가
- ❌ Markdown 미지원 → 가독성 낮음

### After
- ✅ **코드 우선 표시** → 맥락 파악 쉬움
- ✅ **페이지네이션** → 빠른 탐색 가능
- ✅ **미션별 탭** → 각 레포 독립 분석
- ✅ **Markdown 렌더링** → 서식 유지

---

## 🔧 기술 스택

- **React 18.3.1** - UI 프레임워크
- **TypeScript 5.9.3** - 타입 안전성
- **react-markdown** - Markdown 렌더링
- **remark-gfm** - GitHub Flavored Markdown
- **rehype-raw** - HTML 태그 지원
- **D3.js** - 워드클라우드 시각화
- **Supabase** - 데이터베이스

---

## 📝 향후 개선 제안

1. **필터링 기능**
   - 리뷰어별 필터
   - 날짜 범위 필터
   - 키워드 검색

2. **통계 대시보드**
   - 미션별 리뷰 개수 차트
   - 시간대별 리뷰 트렌드
   - 리뷰어 활동 순위

3. **북마크 기능**
   - 중요한 리뷰 저장
   - 나중에 읽기

4. **다크 모드**
   - 초록 테마 유지하면서 다크 모드

---

**개선 완료일:** 2025년 1월
**개발자:** Claude + 사용자
**테마:** 초록 (Emerald) - 흰색 - 검정색
